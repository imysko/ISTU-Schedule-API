{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  },
  "ConnectionStrings": {
    "ScheduleDB": "${ScheduleDB}"
  },
  "TelegramConfig": {
    "ApiId": "${ApiId}",
    "ApiHash": "${ApiHash}",
    "PhoneNumber": "${PhoneNumber}",
    "ChatBotId": "${ChatBotId}",
    "LoginCode": "${LoginCode}",
    "Password": "${Password}"
  }
}